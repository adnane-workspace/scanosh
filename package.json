{
  "name": "scanosh",
  "version": "1.0.0",
  "private": true,
  "author": "Adnane EL MENOUAR",
  "description": "Scanosh — digital QR menus for cafes and restaurants",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:superadmin": "npm run db:superadmin -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "cloudinary:purge": "npm run cloudinary:purge -w backend",
    "db:clear-images": "npm run db:clear-images -w backend",
    "test": "npm test -w backend && npm test -w frontend"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
